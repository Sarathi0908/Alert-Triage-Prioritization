[pytest]
# Run from the slot root:  py -m pytest -q
testpaths = tests
# tests/conftest.py puts the slot root on sys.path so `from src...` resolves
# whether pytest is invoked from here or from a parent directory.
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
